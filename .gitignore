# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Vite dev-server transient logs (kept out of git per audit Phase 4)
vite-dev.err
vite-dev.log

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# No markdown files in this repo. Standing user rule: every committed `.md` was deleted on
# 2026-05-14 and any regenerator (e.g. `scripts/scan-*.mjs`) has been disabled. Anything that
# does sneak through will not be staged because of this ignore. Cursor rule files end in
# `.mdc` and live under `.cursor/rules/`; those are still allowed.
*.md

# TypeScript incremental / project references output (regenerated by `tsc -b`)
*.tsbuildinfo

# Design/input scan JSON emit — local tooling only (do not commit)
scripts/output/

