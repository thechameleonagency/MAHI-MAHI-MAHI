import type { Blockage, ProjectTimelineStatus, Ticket } from "@/types/blockage";

// ============ PURGED: All seed data removed for clean state ============

export const initialProjectTimelineByProjectId: Record<string, ProjectTimelineStatus> = {};
export const initialOperationalBlockages: Blockage[] = [];
export const initialOperationalTickets: Ticket[] = [];
