/**
 * DS8 — Sheet dismiss controls
 *
 * - **Top-right X** — always from `SheetContent` (built-in).
 * - **Bottom-left Cancel** — required on any sheet with unsaved user input (`AppSheetFormFooter`).
 * - **Close / Done** — read-only detail, success confirms, or export previews only (no draft fields).
 */

export const FORM_SHEET_FOOTER_COMPONENT = "AppSheetFormFooter";
