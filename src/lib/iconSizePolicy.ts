/**
 * DS9 — Layout chrome icon sizes
 *
 * Sidebar, header, global search, and notification bell use **16px** (`h-4 w-4`).
 * Do not mix `h-3.5`, `h-5`, or `sm:h-[18px]` on lucide icons under `src/components/layout`.
 *
 * Import `ICON_CLASS_NAV` / `ICON_CLASS_NAV_MENU` from `iconSizes.ts`.
 */

export const LAYOUT_ICON_SIZE = "h-4 w-4";
